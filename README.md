# project
upload file => index.html로 적혀있으면 불가능하다 git 으로 업로드가

setting -> page -> source :deploy from branch ->branch : main -> save. 위에 visit-site. 수정후엔 다시 reload해주고 사이트 들어가면 변해있음 tip!!! 여기서 github에서 배포를 하려면 무조건 파일은 html하나가 있어야 배포할수 있다.
